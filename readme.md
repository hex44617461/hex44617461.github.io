# hex44617461 - Next.js GitHub Pages 블로그

Next.js, React, TypeScript를 사용하여 만든 정적 블로그입니다. GitHub Pages에 완벽하게 최적화되어 있습니다.

## 🚀 주요 기능

- ✅ **마크다운 기반** - `_posts` 폴더에 `.md` 파일을 추가하기만 하면 됩니다
- 🏷️ **카테고리 & 태그** - 포스트를 체계적으로 분류하고 검색할 수 있습니다
- 🌓 **다크/라이트 모드** - localStorage에 테마 설정이 자동으로 저장됩니다
- 📊 **자동 읽기시간 계산** - 포스트의 예상 읽기 시간을 자동으로 계산합니다
- 📸 **이미지 자산 관리** - `_assets/images` 폴더에서 이미지를 관리합니다
- 📱 **반응형 디자인** - 모든 기기에서 완벽하게 표시됩니다
- ⚡ **빠른 성능** - 정적 HTML로 생성되어 매우 빠릅니다
- 📚 **좌측 사이드바** - 네비게이션, 프로필, 소셜 링크가 고정되어 있습니다
- 📋 **우측 사이드바** - 포스트 세부 페이지에서 최근 업데이트, 인기 태그, 목차를 표시합니다

## 📁 프로젝트 구조

```
.
├── _posts/              # 마크다운 포스트 (YYYY-MM-DD-title.md)
├── _assets/images/      # 이미지 자산
├── app/                 # Next.js App Router
│   ├── page.tsx         # 홈페이지 (최신 포스트)
│   ├── about/page.tsx   # 소개 페이지
│   ├── archives/        # 연도별 아카이브
│   ├── categories/      # 카테고리 페이지
│   │   ├── page.tsx     # 카테고리 목록
│   │   └── [category]/  # 카테고리별 포스트
│   ├── tags/            # 태그 페이지
│   │   ├── page.tsx     # 태그 목록
│   │   └── [tag]/       # 태그별 포스트
│   ├── posts/[id]/      # 포스트 상세 페이지
│   ├── layout.tsx       # 루트 레이아웃
│   └── globals.css      # 전역 스타일 (다크모드 포함)
├── components/          # React 컴포넌트
│   ├── Navigation.tsx   # 좌측 사이드바 (테마 토글)
│   ├── Footer.tsx       # 푸터
│   ├── PostCard.tsx     # 포스트 카드 컴포넌트
│   ├── PostSidebar.tsx  # 우측 사이드바
│   └── TableOfContents.tsx # 포스트 목차
├── lib/                 # 유틸리티 함수
│   └── posts.ts         # 마크다운 파싱, 포스트 데이터 처리
├── public/              # 정적 파일
├── next.config.js       # Next.js 설정 (static export)
├── tsconfig.json        # TypeScript 설정
├── tailwind.config.js   # Tailwind CSS 설정
└── package.json         # 프로젝트 의존성
```

## 📝 포스트 작성

`_posts` 폴더에 마크다운 파일을 만들고 다음 형식으로 작성합니다:

```markdown
---
title: "포스트 제목"
date: 2024-01-15
categories: ["카테고리1", "카테고리2"]
tags: ["태그1", "태그2", "태그3"]
excerpt: "포스트 요약 (선택사항)"
---

# 포스트 제목

포스트 본문을 마크다운으로 작성합니다.

## 소제목

더 자세한 내용...
```

### Front Matter 옵션:
- `title` (필수): 포스트 제목
- `date` (필수): 발행 날짜 (YYYY-MM-DD 형식)
- `categories` (선택): 카테고리 배열
- `tags` (선택): 태그 배열
- `excerpt` (선택): 포스트 요약
- `updateDate` (선택): 수정 날짜 (없으면 파일 수정 시간 자동 사용)

## 🖼️ 이미지 사용

`_assets/images/` 폴더에 저장 후 마크다운에서:

```markdown
![설명](/_assets/images/filename.png)
```

## 🛠️ 시작하기

```bash
# 설치
npm install

# 개발 서버 실행
npm run dev

# 빌드
npm run build
```

## 🚀 배포

GitHub Actions를 통해 자동 배포됩니다. `main` 브랜치에 푸시하면 됩니다:

```bash
git add .
git commit -m "Update blog"
git push origin main
```

## 📋 페이지 구조

- `/` - 홈페이지 (최신 포스트)
- `/posts/[id]` - 포스트 상세 (좌측 목차, 우측 사이드바 포함)
- `/categories` - 카테고리 목록
- `/categories/[category]` - 카테고리별 포스트
- `/tags` - 태그 목록
- `/tags/[tag]` - 태그별 포스트
- `/archives` - 연도별 아카이브
- `/about` - 소개 페이지

## 🎨 커스터마이제이션

### 색상 변경
- **프로필 아이콘 배경**: [components/Navigation.tsx](components/Navigation.tsx) 라인 77
- **주요 컬러** (`#446174`): 메뉴, 버튼, 링크 호버
- **다크모드 색상**: [app/globals.css](app/globals.css)의 `html.dark` 섹션

### 소셜 링크 변경
[components/Navigation.tsx](components/Navigation.tsx)의 `socialLinks` 배열:
```typescript
const socialLinks = [
  { name: 'github', url: 'https://github.com/yourname', icon: 'fab fa-github' },
  { name: 'youtube', url: 'https://youtube.com/@yourname', icon: 'fab fa-youtube' },
  { name: 'linkedin', url: 'https://linkedin.com/in/yourname', icon: 'fab fa-linkedin-in' },
];
```

### 프로필 정보
[app/about/page.tsx](app/about/page.tsx)에서 프로필 내용 수정

## 🔧 기술 스택

| 항목 | 버전 | 설명 |
|------|------|------|
| **Next.js** | 16.1.1 | React 프레임워크 |
| **React** | 19.2.3 | UI 라이브러리 |
| **TypeScript** | 5 | 타입 안정성 |
| **Tailwind CSS** | 4 | 유틸리티 기반 스타일 |
| **gray-matter** | 4.0.3 | Front Matter 파싱 |
| **remark** | 15.0.1 | 마크다운 처리 |
| **remark-html** | 16.0.1 | 마크다운 → HTML 변환 |
| **@tailwindcss/typography** | 0.5.19 | Prose 스타일 |

### 추가 기능
- **정적 생성 (Static Export)**: GitHub Pages 호환
- **다크모드**: localStorage 기반 테마 저장
- **자동 읽기시간**: 포스트 단어 수 기반 계산 (200 WPM)
- **파일 수정 감지**: `updateDate` 자동 업데이트
- **Font Awesome 6.4.0**: 아이콘 라이브러리

## 📝 라이선스

MIT

Happy blogging! 🚀
